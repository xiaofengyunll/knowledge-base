# 物理层标准
![[Pasted image 20260605095424.png]]

![[Pasted image 20260605095439.png]]

重点关注：
- 同轴电缆只能半双工
- 双绞线
	- 速率 < 2.5Gbps 可支持半双工 or 全双工（节点连接时协商）
	- 速率 ≥ 2.5Gbps 仅支持支全双工
- 光纤只支持全双工

---
# MAC 层标准
**IEEE 802.3标准的以太网MAC帧**
![[Pasted image 20260605100012.png]]

**V2 标准的以太网MAC帧**
![[Pasted image 20260605100102.png]]

---
# 单播帧、广播帧如何传播？

**单播帧** ：局域网中所有站点都会收到，但是仅有目标站点才会接收帧。
**广播帧**（目前地址全为1）： 局域网中的站点都能收到并且接收帧。

注：路由器收到广播帧后，不会再转发至其他网络。只有同一个局域网内的各节点才属于同一个“广播域”。
MAC地址是数据链路层的概念。路由器、交换机都有MAC地址，而集线器没有MAC地址。

**冲突域**：如果两个节点同时发送数据，会导致冲突，则二者处于同一个“冲突域。
**广播域**：如果一个节点发送广播帧，可以被另一个节点接收，则二者处于同一个“广播域“。

路由器既隔离冲突域，也隔离广播域。
集线器不隔离冲突域，也不隔离广播域。